import { State } from './types';

export const INITIALIZE_STATE: State = Object.freeze({
  operand1: '',
  operand2: '',
  operator: '',
  result: '',
});

export const INITIAL_CALCULATE_VALUE = '0';
